Place static files that should be served at the project root (Vite dev server serves files from the project root).

To use the company logo, put the logo file at `frontend/public/logo.png`. The Navbar uses `/logo.png`.
